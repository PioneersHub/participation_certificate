# Data input

The pipeline now supports three cert types, each with its own source format:

- **Attendee** — CSV
- **Masterclass** — XLSX (Excel)
- **Speaker** — JSON

The exact column / key requirements for each, plus how each field maps onto the `Attendee` model and how UUIDs are derived, live in the canonical walkthrough:

[Walkthrough §3 — Data sources & columns](walkthrough.md#3-data-sources-columns)

That page is the single source of truth. This file is kept only as a stub so existing links keep resolving; do not duplicate the column mappings here.
